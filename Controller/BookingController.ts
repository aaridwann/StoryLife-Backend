const bookingDb = require('../Models/BookingModels')
const projectDb = require('../Models/ProjectModels')

export const booking = async (req: any, res: any) => {

    const{eventName,evntId,location,bookingDate,eventDate,idVendor,vendorName,vendorAddress} =req.body

    // cek event 

    // cek vendor
    
    // cek User

}