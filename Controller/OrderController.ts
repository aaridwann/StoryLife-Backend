const orderDb = require('../Models/OrderModels')
import { BookingInformation, Vendor,Client } from "./BookingController"



export const addOrder = async (booking:BookingInformation,vendor:Vendor,client:Client) =>{
    // const prder = await
}